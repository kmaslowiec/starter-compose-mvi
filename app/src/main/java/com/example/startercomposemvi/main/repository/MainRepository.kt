package com.example.startercomposemvi.main.repository
//TODO replace with your call api implementation
/*
import com.example.startercomposemvi.common.api.CallApi
import javax.inject.Inject

class MainRepository @Inject constructor(private val api: CallApi) {

    //TOD replace with your repository implementation
    */
/*suspend fun getQuestions(question: String) =
        runCatching {
            withContext(Dispatchers.IO) {
                api.getChatCompletions(
                    ChatRequest(
                        messages = listOf(
                            Message(role = "system", content = SYSTEM_INSTRUCTION),
                            Message(role = "user", content = question),
                        )
                    )
                )
            }
        }*//*

}
*/
